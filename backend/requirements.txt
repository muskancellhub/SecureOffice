fastapi==0.136.3
starlette>=0.49.1  # explicit floor: PYSEC-2026-161 / CVE-2025-62727 fixed in 0.49.1
uvicorn[standard]==0.35.0
sqlalchemy==2.0.44
psycopg2-binary==2.9.10
pydantic==2.11.9
pydantic-settings==2.10.1
python-jose[cryptography]==3.5.0
# Direct dependency: AES-256-GCM for per-tenant PII encryption (docs/PII_ENCRYPTION.md).
# Previously present only transitively via python-jose; pin it explicitly so the
# encryption layer doesn't depend on jose's extras to pull it in.
cryptography>=44,<49
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
authlib==1.6.12
httpx==0.28.1
email-validator==2.3.0
dnspython==2.8.0
python-multipart==0.0.32
itsdangerous==2.2.0
openpyxl==3.1.5
crewai[tools]>=1.13.0
resend>=2.0.0
